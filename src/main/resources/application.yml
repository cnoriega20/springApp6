spring:
  datasource:
    url: jdbc:mysql://${springboot_webapp6_db:localhost}:3306/springboot_book_db
    username: root
    password: ####
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
